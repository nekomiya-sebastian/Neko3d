test 3d editor thing idea
	can create points
	connect points to create a face
	select a point by clicking (or worst case scenario cycling through with scroll wheel or smth)
		use wasdRF to move point in space
		click and drag to select many points and transform all at once?
	can rotate scene and maybe fly around
	can save and export model
	maybe check to make sure we don't create multiple identical faces
	
	we can maybe even extend this into an animation system at some point with frames and all that

maybe chiptune style music editor? like with a few preset sfx of drums and instruments and such
	saves in custom format that can be run by the game
	this is an enormous ask lol


main menu with list of all the demo scenes you can click to load
colors string -> index into colors list
forward direction - can just use init forward direction ( 0,0,1 ) and apply the camera's rotation mat to it
raycasting idea - make a super long rectangle emanating from the camera, check what it overlaps with,
	return either all of them or the closest one